Let's optimize the workflow for workspace use a bit. My ideas:

- Re-evaluate the current possible states for status and lifecycle_stage of a workspace
- Make those states configurable, so a workspace is adaptable
- Design and implement commands to close a workspace (set it to final), so no further experiments are possible.