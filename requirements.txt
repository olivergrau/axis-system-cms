# Core numerical support (lightweight, optional but useful)
numpy>=1.26,<3.0

# Data validation / structured models
pydantic>=2.0,<3.0

# Testing
pytest>=8.0,<9.0

# Optional: richer test assertions
pytest-cov>=4.0,<5.0

# Optional: configuration handling
pyyaml>=6.0,<7.0

# Optional: better typing support at runtime (only if needed later)
typing-extensions>=4.8,<5.0

PySide6>=6.6,<7

questionary>=2.0,<3.0

rich>=13.7,<14.0

ruamel.yaml>=0.18,<0.19